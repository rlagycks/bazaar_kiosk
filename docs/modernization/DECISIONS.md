# 현대화 결정 기록

최종 업데이트: 2026-09-07

비즈니스 동작, 아키텍처, 보안, 데이터, 릴리스 범위 또는 Git 거버넌스에 영향을
주는 선택 사항은 이 파일에 기록합니다. 대체된 항목도 보존하고 이를 대신한
결정을 연결하세요.

## 결정 대기 목록

| ID | 결정 | 상태 | 다음 작업 전에 필요 |
| --- | --- | --- | --- |
| D-001 | 장기 기준 브랜치: `develop` 유지, `main`으로 이동, 별도 v2 이력 시작 중 선택 | 대기 중 | 원격 Git 정리; 이번 2A PR은 현 기본 develop 대상(D-026) |
| D-002 | 노출 및 식별 모델: 행사 LAN, 인터넷, 기명 사용자/기기 또는 공유 역할 중 선택 | 대기 중 | 보안 경계 확정 |
| D-003 | 주문, 카운터, 주방, 관리자, 통계 및 API 작업의 권한 매트릭스 | 대기 중 | 보안 경계 확정 |
| D-004 | 일일 주문 번호 초기화, 고유성, 시작 번호, 시간대 및 표시 계약 | 대기 중 | 주문 무결성 확보 |
| D-005 | 현금/티켓/복합 결제, 거스름돈, 취소, 환불 및 부족 결제 규칙 | 대기 중 | 주문 무결성 확보 |
| D-006 | Compose PostgreSQL 방향(D-020)의 지원 버전·런타임·실제 토폴로지 상세 | 대기 중 | 테스트 기반 및 릴리스 설계 |
| D-007 | 최대 부하, 대상 브라우저 및 지연/복구 목표. 기기 방향은 D-025에서 부분 확정 | 대기 중 | 성능 및 프런트엔드 단계 |
| D-008 | 기존 주문, 마이그레이션 및 URL의 보존·호환성 요구사항 | 대기 중 | 데이터/API 변경 |
| D-009 | 서버 렌더링 바닐라 JS 유지 또는 별도 프런트엔드 마이그레이션 검토 | 대기 중 | 프런트엔드 단계 |
| D-010 | 자체 SSE의 이벤트/역할 범위·세션 회수·재접속/폴링 계약과 전환 전 외부 노출 정리(D-018) | 대기 중 | 콘텐츠/Realtime 보안 및 Realtime 신뢰성 단계 |
| D-011 | 주문 상품 및 상태에 대한 Django 관리자 수정을 허용, 금지 또는 서비스 계층 경유 중 선택 | 대기 중 | 재무 무결성 단계 |
| D-012 | 레거시 `received_amount`와 null 허용 분할 결제 필드 정합성 확보 | 대기 중 | 재무 무결성 및 보고 단계 |
| D-013 | 대시보드 기본 기간 및 선택 기간/시간대 동작 | 대기 중 | 보고 계약 단계 |

## 분석으로 구체화한 대기 결정과 담당

다음은 2026-09-06 분석에서 추가한 근거·선택지다. **모두 pending이며 사용자 승인으로
해석하지 않는다.** 담당은 결정 책임 역할이고, 개별 인물 배정은 다음 세션에서 확인한다.
초기 D-P02는 보존한다. 아래 대기 목록과 별개로, 추가 사용자 메시지에서 직접 지정한
자체 SSE 방향은 D-018 accepted, 후속 Compose PostgreSQL 운영 방향은 D-020 accepted다.
상세 SSE D-019와 DB 이전 D-022는 pending, EC2 후보 D-021과 단계 제안은 proposed다.

| ID | 상태 | 결정 담당 | 확인된 근거 / 결정할 선택 | 결정 전 독립적으로 가능한 일 |
| --- | --- | --- | --- | --- |
| D-001 | pending | 저장소 소유자 | main/develop 동일 tree, 고유 이력 존재. develop 유지/main 이동/v2 이력 중 선택 | 로컬 ref·고유 콘텐츠 기록; 원격 변경은 별도 승인 |
| D-002 | pending | 서비스 소유자·보안 담당 | 익명 API·기본 PIN·DEBUG 노출(BK-R001/002/028). 노출 LAN/인터넷, 기명/기기/공유 식별 | 현행 인증/세션 특성화, 배포 토폴로지 요청 목록 |
| D-003 | pending | 운영 책임자·보안 담당 | API 역할 없음, H/T도 전체 kitchen 페이지 허용. 목표 역할·객체/mode 범위·매출 조회 권한 | 보고서의 현행 URL/메서드 매트릭스 테스트 설계 |
| D-004 | pending | 운영 책임자·데이터 담당 | PG41→42, SQLite 다음날1, 충돌 재시도 실패. 날짜별/연속·시작값·공백·고유성·Seoul 주문일 | 실측 fixture 보존; 정책 없이 번호 재설정 금지 |
| D-005 | pending | 재무·운영 책임자 | 부족결제201, 실수 잘림, 총수납/거스름/환불 분리 부재. 현금·식권·혼합·초과/부족·취소 환불 규칙 | 현행 금액 경계·원본 필드 특성화; 임의 백필 금지 |
| D-006 | pending | 운영 인프라 담당 | Compose PostgreSQL은 D-020로 지정, EC2는 D-021 후보. 원본/대상 PG·image/CPU·ASGI·풀·DB TLS는 대기. 로컬PG15.18은 운영 기준 승인 아님 | 설치 버전 기록·지원표 확인·기존 CI 공백 지도 |
| D-007 | pending | 행사 운영·프런트·성능 담당 | 주문·서빙 휴대폰, 주방·관리자 PC는 사용자 확정(D-025). 브라우저/접속수·주문량·backlog·p95·복구시간은 미정 | 제안 workload 표 작성, 통과 기준은 미확정 유지 |
| D-008 | pending | 데이터 소유자·운영 담당 | 0018↔0019 제약 실패, 과거 삭제 필드·URL·메뉴명 변경. 보존 기간·마이그레이션 적용이력·호환 범위 | 정제 fixture·이력/행종류 인벤토리 설계; 운영 조회는 별도 범위 |
| D-009 | pending | 제품·프런트 책임자 | 현행 작은 Django/JS 앱. 점진적 유지 또는 안정 API 뒤 부분 UI 교체 | 현행 여정·참조/구문 검사; 전면 교체는 제안하지 않음 |
| D-010 | pending | 보안·실시간·운영 담당 | 전송 방향은 D-018 자체 SSE로 지정됨. 이벤트/역할 범위·인증 회수 지연·재접속/폴링·전환 전 외부 RLS/publication 정리 계약은 대기 | 외부 정책 검증 목록·상태/응답순서 시나리오 |
| D-011 | pending | 운영·재무·관리자 담당 | 관리자 항목 저장 후 합계5000/15000 차이. 추가/수정/삭제/상태/번호를 금지 또는 서비스 경유 | writer 지도·관리자 회귀 fixture 설계 |
| D-012 | pending | 재무·데이터 소유자 | legacy detail5000/SUM null, 혼합 원본에서 분할 복원 불명확. backfill/조회 fallback/미분류 유지 | 원본 보존·합성 legacy 집계 대조 |
| D-013 | pending | 재무·운영 책임자 | fixed day helper, dashboard 별칭500. 오늘/최신행사/명시기간·order_date/created_at·Seoul 경계 | 별칭 오류를 정책과 분리한 실행성 시험 |
| D-014 | pending | 현장 운영 책임자 | 일반 table/포장101~120 슬롯, order_type/is_takeout/item mode 불일치. 슬롯 재사용·혼합 보드·flag 조합 | 현재 허용/거부 조합과 제약 충돌 fixture |
| D-015 | pending | 주방·재무·운영 책임자 | 취소→활성200, 수량기반/직접 상태 writer 공존. 취소 종결/복구권한·완료/준비 전이 | 상태 전이표 초안과 경쟁 요청 시험 설계 |
| D-016 | pending | 서비스 소유자·운영 담당 | 백업/복원·RPO/RTO·장애연락·수동 처리·출시/복구 증거 미확인 | 필요한 운영 증거 목록·복원 rehearsal 설계 |
| D-017 | pending | 데이터 소유자·운영·저장소 관리자 | 빈0020 실패와0018↔0019 제약 실패. 신규 bootstrap/대체 체인/정확히 승인된 과거 산출물 수정 전략 | 적용 이력·원본 보존·신규/기존 경로 비교; 과거 파일 수정 금지 |

D-014~017은 이번 분석에서 새로 추가한 결정이다. 모두 날짜 2026-09-06, 상태 pending.
금전/영속 데이터/보안 동작에 영향을 주므로 코드에서 임의로 정책을 선택하지 않는다.
D-017은 과거 번호·포장 슬롯·필드 소실 가능성과 구 애플리케이션/새 스키마 호환성도 포함한다.

### D-018 — 브라우저 외부 Realtime을 자체 SSE로 전환

- 상태: accepted (사용자가 직접 지정한 방향)
- 날짜: 2026-09-06
- 결정 제공: 저장소 사용자; 후속 통합 담당은 실시간·백엔드·프런트 담당
- 사용자 지침: “기존에 브라우저에 보내던 방식을 외부 서드파티 연결 안하고 직접 sse 방식으로
  리펙토링도 하려고 … 이것도 같이 분석해서 포함해줘”
- 결정 범위: 브라우저의 Supabase Realtime/CDN SDK 의존을 제거하고 자체 서버 SSE로 대체하는
  방향을 현대화 분석에 포함한다. 현재 실제 교체 화면은 주방이며, 쓰기 명령은 HTTP로 유지하는
  방안을 제안한다. 이 SSE 요청 자체로 카운터의 새 자동갱신이나 DB 호스팅 이전을 추정하지 않았다.
  이후 사용자가 DB 자체 운영을 추가 지정한 내용은 별도 D-020에 기록한다.
- 승인 경계: 이번에는 분석 문서만 수정한다. 코드·스키마·의존성·배포·외부 설정 변경이나
  원격 push/merge를 승인한 것이 아니다. 같은 SSE 방향을 다시 묻지 않는다.
- 결과: D-010의 전송 방식 선택은 이 결정으로 구체화했다. RLS 정리·새 권한 경계와 D-019는
  남는다. 안전한 자체 API 폴링을 fallback/롤백으로 제안하고 외부 SDK 재활성화를 기본값으로 삼지 않는다.
- 근거/검증: [SSE 추가 분석](ANALYSIS_REPORT.md#sse-migration)의 파일 지도와 로컬 실행 조건.
  실제 SSE endpoint·DB revision·브라우저/프록시는 구현·검증하지 않았다.

### D-019 — 자체 SSE의 영속 변경 감지·복구·운영 상세 계약

- 상태: pending
- 날짜: 2026-09-06
- 담당: 실시간·데이터·보안·운영 통합 책임자; 이벤트 보존 요구는 현장 운영 책임자
- 결정 필요 시점: 프롬프트02에서 단계/의존성을 정리하고, 해당 SSE 구현 단계 전에 확정
- 우선 제안: 동일 출처 EventSource, Django async 스트림, 거래에 포함된 영속 revision,
  워커당 공유 확인 허브, 권한이 적용된 일관된 snapshot. 별도 외부 메시지 서비스는 도입하지 않는다.
- 핵심 선택: 최신 대기 상태 복구만 필요한지, 모든 중간 전이·소리 알림·감사 재생이 필요한지.
  전자는 revision 우선, 후자는 outbox와 보관/재생/중복 계약을 비교한다.
- 구현 관문: 서비스 writer 통합 대 DB trigger, 전역 대 범위별 revision·old/new 대상,
  외부 SQL/SQLite 개발 경로, revision+도메인 잠금 순서·멱등 전체 재시도, snapshot 격리/캐시,
  generation 복원 절차·허브 수명. DB trigger나 Redis/Channels를 자동으로 승인하지 않는다.
- 연결 관문: CONNECTING의 native 재시도와 CLOSED의 제한된 재생성, 인증 fetch·세션 저장소
  오류 시 발행 상한, 역할 변경 때 큐/화면/이전 응답 폐기, 같은 generation의 revision 역전 방어.
  폴링 종료는 heartbeat 수신이 아니라 감지 허브 정상과 완전한 snapshot 적용으로 판단한다.
- 기존 대기 결정과 연결: D-003의 역할별 payload/발생 빈도 노출, D-006의 ASGI·DB풀·프록시,
  D-007/010의 지연·heartbeat·폴링/재시도·탭 수명·인증 회수 목표, D-008/011의 writer 범위,
  D-016의 복원/롤백을 확정해야 한다. 1초 revision poll·15초 heartbeat는 비교용 제안값이다.
- 검증: BK-R035~040의 writer·다중 워커·커밋/응답 역전·권한 회수·프록시·부하 인수 기준.
  로컬 async 응답 직접 순회는 실제 SSE 구현 통과를 대신하지 않는다.
- 독립 진행 가능: 기존 권한/조회/writer 특성화와 청사진 분할, 외부 연결 제거 대상 지도,
  재접속·완전한 대기 목록·안전한 폴링의 검증 설계.

### D-P04 — 자체 SSE의 단계 분할과 안전한 교체 경로

- 상태: proposed
- 날짜/담당: 2026-09-06, 기술·운영 통합 담당
- 근거: [SSE 추가 분석](ANALYSIS_REPORT.md#sse-migration), BK-R035~040, D-018.
- 제안: 3/4A 인가·세션, 5/6/7 writer, 8/9 snapshot 계약을10 SSE로 연결한다.
  12A의 최소 ASGI/프록시 실행 조건은10에 선행시킨다. 4B CDN/키 제거와10/11 주방 교체는
  한 통합 담당이 조정하고, 선행 제거가 필요하면 먼저 보호된 자체 폴링을 제공한다.
- 비용/대안: revision 확인은 broker 운영을 줄이지만 DB 읽기·공유 잠금·전체 재조회 비용이
  생긴다. LISTEN/NOTIFY는 선택적 깨우기이며 영속 상태를 대체하지 않는다. outbox는 이력 요구 시 비교.
- 롤백: 새 revision을 쓰지 않는 구앱은 SSE를 끄고 호환 자체 폴링으로 복귀해야 한다.
  인증·CSRF·출력 보호를 유지하며 외부 구독 복원이나 과거 migration 수정을 승인하지 않는다.
- 다음 작업: 프롬프트02에서 실제 PR 크기·파일 소유권·위험 매핑과 조건부 관문을 검토한다.
  SSE 추가 시점40개에서 인프라 추가 후44개로 확장된 내용은 D-P05와 함께 따른다.
  이번에는 BLUEPRINT를 수정하지 않는다.

### D-020 — Docker Compose의 PostgreSQL로 DB 자체 운영 전환

- 상태: accepted (사용자가 직접 지정한 방향)
- 기록일: 2026-09-07
- 결정 제공: 저장소 사용자; 담당은 데이터·인프라 통합 책임자
- 사용자 지침: “인프라도 전환할 계획 … docker compose로 postgre db를 올리는 형태로 전환”.
- 결정: 현재 DB 호스팅 유지 가정을 목표에서 바꾸고 Compose PostgreSQL로 이전하는 계획을
  분석에 포함한다. D-018은 브라우저 SSE 전환, D-020은 DB 호스팅/운영 전환을 담당한다.
- 범위: 방향만 지정했다. 원본 DB 실제 호스팅/버전, 대상 PG major/이미지·EC2 리전/사양,
  앱까지 같은 Compose에 둘지, 백업·중단·비용·HA 상세는 D-006/016/021/022에 남는다.
- 승인 경계: 이번은 분석 문서만 수정한다. 실제 Compose/Dockerfile 작성·이미지 변경·AWS 자원
  생성·원본 접속/export·운영 DB 이전/배포·원격 push/merge를 승인한 것이 아니다.
- 영향: 직접 맡을 patch/백업/복원·용량/장애 대응과 기존 데이터·sequence·SSE generation의
  이전을 분석한다. 자체 SSE와 Compose 선택을 다시 확인하는 질문은 필요 없다.
- 근거/검증: [인프라 추가 분석](ANALYSIS_REPORT.md#infrastructure-migration), E-INFRA-STATIC.
  실제 DB 복사·Compose/AWS 실행은 하지 않았다.

### D-021 — EC2 배포와 단일 호스트 구성을 우선 비교 후보로 검토

- 상태: proposed (사용자가 EC2를 검토 후보로 제시, 확정 아님)
- 기록일: 2026-09-07
- 담당: 서비스 소유자·인프라·보안·현장 운영 책임자
- 근거: 사용자 “aws 같은곳에 ec2를 생각”; D-020과 자체 SSE 분석.
- 제안: EC2 한 대에서 TLS proxy·Django ASGI·내부 PostgreSQL을 Compose로 실행하고,
  별도 암호화 EBS와 호스트 밖 백업 저장소를 사용하는 최소 구성을 먼저 비교한다.
- 선택 기준: 단일 VM/AZ·행사 인터넷 장애의 중단 시간을 D-007/016이 허용하는지,
  새 호스트 restore와 데이터 보존을 실제로 입증할 수 있는지 확인한다. HA가 필요하면
  별도 호스트/복제 설계를 비교한다. 단일 호스트 장애 위험을 이미 수용한 것으로 보지 않는다.
- 대기 세부: provider/리전·CPU 아키텍처/크기·OS/이미지, DB 버전/TLS·EBS·도메인/인증서,
  SG/SSM/IAM/secret·백업 위치/키·예산/담당·지원 시간. 인스턴스 타입/가격/비용 절감은 미확정.
- 검증: BK-R035/039/040과 BK-R041/043/044의 실제 proxy/SSE·권한/포트·설정·부하·복원.
- 실행 경계: EC2, S3, EBS 등 리소스 생성·과금·운영 설정 변경은 이번 요청에서 실행하지 않는다.

### D-022 — DB 이전의 원본 범위·중단·검증·되돌림 계약

- 상태: pending
- 기록일: 2026-09-07
- 담당: 데이터 이전 통합 책임자, 재무·현장 운영·서비스 소유자
- 결정 필요 시점: target PG/복원 설계를 확정하기 전과 실제 최종 쓰기 전환 전
- 확인할 것: 실제 원본 호스팅/PG 버전·적용 migration·provider 관리 객체/extension/권한,
  기존 데이터 보존 범위·세션 처리·모든 writer·허용 중단 시간·RPO/RTO·원본 보존 기간.
- 제안 절차: 정제 복사본 export/restore와 객체/행/금액/번호 비교, 빈 DB bootstrap과 기존
  schema+data+migration 이력 복원 경로 분리, 최종 writer 동결·단일 DB 쓰기·SSE reset.
- 되돌림 관문: 대상 신규 쓰기 전은 동결 원본 복귀를 검증한다. 새 주문을 받은 뒤에는
  URL/DNS만 되돌리지 않고 역이전/대조 또는 쓰기 중단 후 정방향 복구로 신규 데이터를 보존한다.
  무중단·이중 쓰기·논리복제는 별도 요구와 검증 없이 기본값으로 선택하지 않는다.
- 복원 보안: 폐기된 세션과 옛 SSE generation이 백업에서 복구될 수 있으므로 재개 전 세션
  무효화/새 인증 세대와 서비스 공통의 새 SSE generation을 제안한다. 단순 세션 재조회·
  워커별 새 generation 생성으로 대체하지 않는다. 이전 cookie/cursor 거부를 인수 기준에 넣는다.
- 원본/복원 관문: 엔진이 SQLite이면 별도 데이터 이행 경로를 설계한다. 이미0020·historical0018·
  fresh·data-only 경로를 구분하고 객체 누락/restore 오류를 성공으로 처리하지 않는다.
- 선행 결정: D-004/008/011/016/017/019, D-020. 기존0020/0019 오류를 컨테이너화로 해결한
  것으로 보거나 과거 migration 수정·fake 이력을 임의로 승인하지 않는다.
- 검증: BK-R041/042의 새 호스트 복원·data/sequence·쓰기 동결·전환 전후 복구 기준.

### D-P05 — 컨테이너 기반·DB 이전·운영 인수의 분할

- 상태: proposed
- 기록일/담당: 2026-09-07, 기술·데이터·인프라 통합 담당
- 근거: [인프라 분석](ANALYSIS_REPORT.md#infrastructure-migration), D-020/021/022, 위험44개.
- 제안:1/2의 target PG·bootstrap·이미지/CI,3/4A의 필수 설정·네트워크/권한,
  10 이전의 ASGI/proxy,12A의 EBS/백업복원·정제 이전 rehearsal를 각각 검토 가능한 단위로 나눈다.
  최종 인수12B 뒤 실제 자원 생성·데이터 이전/전환은 별도 승인된 실행 작업으로 남긴다.
- 통합 경계: 데이터 schema/writer/restore/SSE generation은 한 통합 책임자가 조정한다.
  DB 호스팅 이전·도메인 정책 변경·외부 Realtime 교체를 한 번에 배포해야 한다고 가정하지 않는다.
- 롤백: 앱 이미지·DB schema 호환성과 새 쓰기 보존을 함께 확인한다. 자체 API 폴링은 SSE
  기능 저하 경로이며 EC2/DB/행사 인터넷 장애까지 복구하는 오프라인 기능이 아니다.
- 다음 작업: 프롬프트02에서 실제 단계·파일 소유권·위험 매핑·인수 기준에 반영한다.
  이번에는 BLUEPRINT·Compose·Dockerfile·CI·과거 migration을 수정하지 않는다.

### D-P03 — 분석 권고와 청사진 재검토 순서

- 상태: proposed
- 날짜: 2026-09-06
- 담당: 제품/기술 책임자, 통합 담당
- 근거: [ANALYSIS_REPORT.md](ANALYSIS_REPORT.md)의 초기34개 위험과 양DB 재현. 후속 SSE 위험6개는 D-P04에서 연결한다.
- 제안: 점진적 현대화를 유지한다. 단계1에0019/0020의 신규·기존·역방향 경로를 명시하고,
  정책과 분리 가능한 테스트 기반·API 보안·통계 실행 오류의 작은 단위를 앞당길지 검토한다.
  프런트엔드 부분 교체는 계약이 안정된 단계9 이후 다시 판단한다.
- 대안/트레이드오프: v2는 구조 선택 폭이 크지만 테스트0 상태에서 데이터/재무/인가를
  모두 다시 증명해야 한다. 부분 교체도 현재 계약 오류를 먼저 해결해야 한다.
- 마이그레이션/롤백: 어떤 제안도 과거 파일 수정, 인증 경계 복원, 운영 데이터 변경,
  push/merge 또는 배포를 승인하지 않는다.
- 검증/다음 작업: [prompts/02_REVIEW_BLUEPRINT.md](prompts/02_REVIEW_BLUEPRINT.md)에서
  새 위험-단계 매핑·실제 선행 조건·PR 크기·보안 유지 rollback을 검토한다.
- 기존 결정 관계: D-P01의 proposed 점진적 방향을 구체화하며 승인 상태로 승격하지 않는다.

### D-023 — 주문·서빙 / 주방 / 관리자 중심으로 기능·명칭 정리

- 상태: accepted (사용자가 명시한 기능 구분과 명칭 방향)
- 날짜: 2026-09-07
- 결정 제공: 저장소 사용자; 담당: 제품·프런트·백엔드 통합 담당
- 사용자 지침: “이름을 정리할 필요가 있다는 점은 동의해 주문·서빙 / 주방 / 관리자 중심으로
  정리하자 문서에도 반영해줘”.
- 결정: 사용자에게 보이는 기능 구분과 명칭을 **주문·서빙 / 주방 / 관리자** 중심으로 정리한다.
  주문 입력·결제 입력은 주문·서빙, 준비/완료·홀/포장 처리는 주방, 통계와 운영 관리는 관리자
  영역으로 매핑한다. 구체적 메뉴·수정 권한·화면 통합 방식은 기존 결정과 함께 검토한다.
- 현행 매핑: ORDER→주문·서빙, KITCHEN 계열→주방, B1_COUNTER의 실제 판매 통계와
  Django admin의 메뉴/테이블/주문 관리→관리자 영역. 이름이 카운터라는 이유로 통계를 삭제하지 않는다.
- 경계: 세 기능 영역이 곧 로그인 역할 세 개라는 뜻은 아니다. 홀/포장 역할 병합이나 통계
  조회자에게 Django 관리자 수정 권한 부여는 승인하지 않았다. D-003/011의 세부 권한은 대기다.
- 호환성: 기존 URL·역할 코드·주문의 source/floor·번호·과거 데이터는 이름과 구분한다.
  일괄 문자열 치환·과거 migration 수정·기존 주문 재분류를 승인한 것으로 해석하지 않는다.
- 미확정: 지상/부스 기능과 데이터의 유지·제거 범위는 D-024로 남긴다. 지상을 지하의 오타로
  단정하지 않으며, 이번 승인은 세 기능 영역 중심의 정리 방향이다.
- 검증/인계: [기능·명칭 매핑](ANALYSIS_REPORT.md#functional-naming)과 기존44개 위험을
  프롬프트02에 연결한다. 이번에는 문서만 수정하며 코드·인프라·push/merge는 실행하지 않는다.

### D-024 — 지상·부스 정리와 관리자 세부 기능 범위

- 상태: pending
- 날짜/담당: 2026-09-07, 제품·현장 운영·데이터 소유자
- 결정 필요 시점: 지상/부스 관련 UI·필드 제거 또는 역할/URL/과거 데이터 변경 전
- 확인된 사실: 현행 활성 화면/역할에 F1/BOOTH가 없고 신규 주문은 B1·매장/포장만 받는다.
  visible_booth는 관리자 편집 항목에 남지만 현행 메뉴 API/생성에서 사용하지 않는다.
  운영 DB의 과거 지상/부스 행 존재 여부는 미확인이다.
- 남은 범위: 지상 기능 일부 유지 여부, 부스 완전 종료 여부, 과거 주문·통계 조회/보존,
  관리자 통계 조회와 메뉴/테이블/주문 수정 권한, 홀/포장 주방의 별도 화면/역할 유지 여부.
- 제안: 통계를 관리자 영역으로 식별한 뒤 사용하지 않는 부스 UI·명칭부터 정리할 단위를
  검토한다. DB 필드 삭제는 사용/데이터 인벤토리·정방향 migration·롤백 검증 뒤 별도 결정한다.
- 독립 진행: D-023의 명칭표·기능 매핑과 URL/권한 호환 계획은 삭제 범위 확정 전에도 작성 가능하다.
- 관계: D-003/008/009/011/013/014, BK-R008/016/023/024/027/031/034.

### D-025 — 기존 업무 흐름 보존과 디자인 작업의 후속 분리

- 상태: accepted (사용자가 직접 지정한 작업 순서·기기·흐름 범위)
- 기록일: 2026-09-07
- 사용자 근거: “플로우는 일단 지금을 그대로”, “주방과 관리자 … 둘다 pc”,
  휴대폰 서빙 요청, “디자인쪽은 추후에 수정하고 … 현대화 작업에서 순서 정리”,
  이어서 청사진 재검토를 제안한 응답에 “다음 단계 진행하자”.
- 결정: 이번 작업은 프롬프트02의 실행 계획·문서 검토다. 주문·서빙은 휴대폰,
  주방·관리자는 PC로 검증하며 기존 업무 흐름을 보존한다. Figma 수정·디자인 적용과
  새 제품 요구사항은 후속 UI 작업으로 분리한다.
- 경계: 앱 구현·전체 청사진 실행·DB/인프라 이전·push/merge 승인은 아니다.
  화면 기기를 확정한 것이 D-007의 부하·브라우저·성능 목표까지 확정한 뜻은 아니다.
  잘못된 보안·금액 동작을 정상 계약으로 승인하지 않으며 관련 정책은 기존 결정에 남긴다.
- 인계: [BLUEPRINT.md](BLUEPRINT.md)의11은 현행 화면 안정화, UI는 보류된 후속 작업이다.
  기존 [Figma 산출물](UI_UX_REDESIGN.md)은 그대로 보존한다.

### D-P06 — 증거에 맞춘 하위 단계와 실행 관문 재구성

- 상태: proposed (프롬프트02에서 검토·문서 검증한 실행안; 제품 정책 승인 아님)
- 날짜/담당: 2026-09-07, 현대화 통합 담당
- 근거: [분석 보고서](ANALYSIS_REPORT.md), [위험 등록부](RISK_REGISTER.md)의44개 위험,
  독립 검토의 보안 선행 경로·SSE/인프라 의존성 지적.
- 검토 결과: 점진적 Django 현대화를 유지한다. 전면 재작성은 테스트0인 현행에서
  권한·재무·상태·이전 계약을 동시에 다시 검증하게 하므로 현 근거로 권고하지 않는다.
  D-P03/04/05를 계획에 반영했지만 제안 상태와 원래 당시 작업 범위는 보존한다.
- 실행안:2A 로컬 특성화와1A PG fixture를 분리하고3/4A1/8A를 앞당긴다.
  PG 설치 복구1B와 CI2B의 최종 관문은 유지한다.7A/B/C의 결제·관리자·레거시,
  10A/B/C/D1/D2/E의 ASGI·영속 감지·snapshot·서버·클라이언트·부하,
  12A1/2/3의 운영 설정·복원·이전 리허설을 각각 검토 가능한 단위로 나눈다.
- 종료·통합: 모든44개 위험에 주 담당 하위 단계1개와 종료 증거를 연결한다.
  코드 단위 완료와 외부 운영 증거를 포함한 위험 최종 종료를 구분한다.
  schema/writer/감지·DB 이전 통합 책임은 한 명이며 공유 파일은 무조건 병렬 편집하지 않는다.
- 미정 유지: D-002~017/019/022/024의 제품·데이터 계약, D-021 EC2 후보.
  단계별 관문만 적용하고 이미 받은 SSE·Compose·명칭 선택을 다시 묻지 않는다.
- 영향: 다음 구현 후보는2A다.2A는 운영 DB·번호/결제/권한 변경 없이 시작할 수 있다.
  정확한 실행 범위는 [청사진](BLUEPRINT.md#phase-2a)과 [프롬프트03](prompts/03_IMPLEMENT_PHASE.md)을 따른다.
  실제 운영 전환R은12B 감사와 구체적인 별도 사용자 승인 이후다.
- 검증: 위험 매핑 누락/중복, 선행 단계 존재·DAG, 문서 링크·렌더링·허용 파일을 확인한다.
  이 계획 갱신으로 위험을 Closed로 바꾸거나 운영 검사를 통과시킨 것은 아니다.

### D-026 — 첫 구현 2A와 이슈·전용 브랜치·PR 게시

- 상태: accepted (사용자가 지정한 구현·게시 범위)
- 기록일: 2026-09-07
- 사용자 근거: “첫 구현 이슈 브랜치 따서 진행하고 pr 까지 올려줘”.
- 승인 범위: 제안한 첫 단계2A의 격리 테스트 구현·검증·인계, GitHub 이슈 생성,
  전용 브랜치와 로컬 커밋, 해당 브랜치 push 및 PR 생성.
- 구현상 선택: GitHub에서 확인한 현 기본 브랜치 develop을 이번 PR 대상으로 사용한다.
  main/develop 최신 원격 head는 각각 bca9e40/93a841a로 기존 확인과 같다.
  장기 기준 브랜치 정책 D-001, 원격 이력 정리·삭제·설정 변경은 결정하지 않는다.
- 인계 보존: 기존 로컬 준비 커밋 위에서 시작하고 미커밋 분석·청사진을 별도 문서
  체크포인트03ac108로 보존했다. 이어지는 테스트 커밋과 리뷰 범위를 구분한다.
- 경계: merge, 배포, 실제 인프라/운영 데이터, 과거 migration, 전체 단계 일괄 구현은 제외한다.
  D-025의 기존 흐름·기기·디자인 보류 방향은 유지한다.

### D-027 — 머지된2A 이후1A 구현 진행

- 상태: accepted (다음 단계 구현 범위)
- 기록일: 2026-09-07
- 근거: 첫 구현 이슈·브랜치·PR 요청 뒤 사용자가 “내가 머지 진행했고 다음 단계 진행하자”.
  GitHub PR36 MERGED, develop head ff013b4를 확인했다.
- 범위: 앞서 제안한1A의 로컬 Compose PostgreSQL·전용 테스트 설정·migration 실패 fixture·
  검증·인계. 기존 이슈/전용 브랜치/PR 절차를 이어가고 merge는 사용자에게 남긴다.
- 선택: 기존 로컬 PG15.18 이미지 digest를 재현용으로 사용한다. 운영 버전·EC2 토폴로지·
  원본 데이터 이전을 승인한 것이 아니다. 현재 개발 DB와 분리된 loopback 포트·전용 볼륨을 만든다.
- 경계: 과거0019/0020 수정, 임의 과거 테이블 배정, 데이터 삭제는 포함하지 않는다.
  해당 복구는 D-017/단계1B의 구체적인 수정안·호환 경로 검토가 필요하다.

### D-P07 — 1B의0020 제한적 수정 후보와0019 정책 분리

- 상태: proposed, 적용 승인 대기
- 기록일: 2026-09-08
- 사용자 근거: PR38 머지 후 다음 단계 진행 요청.1B 후보 검토·테스트는 진행하되,
  D-017의 정확한 과거 파일 변경 승인을 대신한 것으로 해석하지 않는다.
- 제안: [전체0020 패치](proposals/0020-empty-sequence.patch)의 빈/NULL/0 초기화 수정과
  이력 없는 기존 sequence의 재설정 거부를 먼저 승인·적용한다.
- 검증: 별도 복사본 PG 후보12개와 기존8개 특성화 통과. 원본 앱/migration 변경 없음.
  원본0020 이미적용/no-op, 기존sequence충돌, 생성 후 실패 원자성까지 확인했다.
- 0019: NOT VALID는 과거행을 남겨도 상태 UPDATE를 막아 현행 앱 호환안으로 채택하지 않는다.
  과거 주문의 조회 전용/운영 수정 여부가 정해질 때까지 기존 실패 중단·행 보존을 유지한다.
- 경계:0020 적용을 승인해도1B 전체 완료나0019 정책/운영 이전 승인으로 확장하지 않는다.
  [복구 검토안](MIGRATION_REPAIR_REVIEW.md)의 미정 경로를 유지한다.

## 결정 기록 템플릿

### D-XXX — 제목

- 상태: 제안됨 | 승인됨 | 거절됨 | 대체됨
- 날짜:
- 담당자:
- 결정 필요 시점:
- 맥락과 근거:
- 검토한 선택지:
- 결정:
- 결과와 트레이드오프:
- 마이그레이션/롤백 영향:
- 검증:
- 대체 대상 / 대체 결정:

## 준비 단계 결정

### D-P01 — 증거 우선 방식의 점진적 현대화를 초안 계획으로 사용

- 상태: 제안됨
- 날짜: 2026-09-06
- 맥락과 근거: 애플리케이션은 현재 구조에서 점진적으로 개선하기에 충분히
  작지만, 재무, 권한 부여, 동시성 동작에 대한 자동화 테스트가 없습니다.
  기존 `main`과 `develop`의 끝점은 그래프가 분기되어 있지만 동일한 소스
  트리를 공유합니다.
- 제안된 결정: 다시 쓰기 여부를 결정하기 전에 특성화 테스트와 프로덕션
  맥락에 대한 근거를 확보합니다. 각 단계는 작게 나누고 변경은 전진 방향으로만
  누적합니다.
- 결과: 청사진은 점진적 작업을 전제로 하지만, 제품 요구사항상 v2 재구축이
  더 안전하다고 입증되면 0단계에서 이 제안을 대체할 수 있습니다.

### D-P02 — 준비 중에는 원격 Git 이력을 변경하지 않음

- 상태: 승인됨
- 날짜: 2026-09-06
- 맥락과 근거: 사용자는 이후 Astra 세션을 위한 준비를 요청했으며, 현재는
  이력을 다시 써야 할 내용 수준의 비밀정보 발견 사항이 없습니다.
- 결정: 로컬에서 클론하고 검사하며 로컬 준비 브랜치만 생성합니다. 원격 참조,
  기본 브랜치, 보호 설정, 태그, 이력은 변경하지 않습니다.
- 검증: 이후 승인된 Git 작업을 수행하기 전에 `git status`, 브랜치/참조
  목록, 원격 상태를 검사합니다.
